# PROMPT: dl-2026-08-17-get-started — reorganizar docs introdutórios em get_started/

> Auto-generated by prompt-crafter (Dev Loop RUN `dl-2026-08-17-get-started`)

---

## Goal

Mover os 10 arquivos `.md` introdutórios da raiz do repositório para `get_started/`, mantendo
apenas `README.md`, `CLAUDE.md` e `CONTEXT.md` como `.md` soltos na raiz, corrigindo **todos** os
links/paths afetados (raiz, `docs/00-INDEX.md`, os próprios arquivos movidos, mirrors
`.cursor/`/`.claude/`/`.github/` e 3 scripts de tooling com paths hardcoded), sem quebrar nenhuma
referência existente.

---

## Quality Tier

**Tier:** production

| Tier | Expectations |
|------|--------------|
| `production` | Sem links quebrados; scripts de validação (`validate-agent-router.py`, `validate-workflow-bundle.py`) devem passar após a mudança; usar `git mv` para preservar histórico. |

---

## Context

- Run Dev Loop: `.claude/sdd/dev-loop-runs/dl-2026-08-17-get-started/`
- Requirements: [`artifacts/dl-2026-08-17-get-started_requirements.md`](../../sdd/dev-loop-runs/dl-2026-08-17-get-started/artifacts/dl-2026-08-17-get-started_requirements.md) (FR-001..FR-008)
- Design: [`artifacts/dl-2026-08-17-get-started_design.md`](../../sdd/dev-loop-runs/dl-2026-08-17-get-started/artifacts/dl-2026-08-17-get-started_design.md) (T1-T19, este PROMPT é a tradução 1:1)
- Reorganização puramente estrutural/textual — sem lógica nova, reversível via git.
- **Fora de escopo (não tocar):** `.claude/worktrees/**` (worktree separado), `install_dev_loop/**` (exceto os 2 assets regenerados via script na task de sync), `agentspec/**`, `templates/*-generico.md`.

### Arquivos que MOVEM para `get_started/` (10)

`START_HERE.md`, `DEV_LOOP_Guia_Comandos.md`, `SDD_Guia_Comandos.md`, `HOW_TO_USE_AGENTS.md`,
`HOW_TO_MAKE_NEW_AGENT.md`, `MANUAL_AGENTES.md`, `AGENTIC_UPGRADE_Guia.md`,
`COMO_TORNAR_PROJETO_AGENTIC.md`, `CRIAR_AMBIENTE_VIRTUAL.md`, `copilot-instructions.md` (o
arquivo-ponte da raiz — **não** o `.github/copilot-instructions.md`, que fica onde está).

### Arquivos que FICAM na raiz

`README.md`, `CLAUDE.md`, `CONTEXT.md`.

---

## Agentes (Dev Loop Executor)

<!--
OBRIGATÓRIO (prompt-crafter + dev-loop-executor): mapa visível de quem invocar com @ no Cursor.
-->

| Etapa / necessidade | Agente (papel) | Caminho |
|---------------------|-----------------|---------|
| Nenhum agente de domínio necessário | — | Tarefa 100% mecânica (mover arquivos + editar paths/links + rodar scripts já existentes). O `dev-loop-executor` executa diretamente com `Read`/`Edit`/`Bash` (`git mv`, `grep`, `python3`, `bash scripts/*.sh`). Nenhuma linha de tarefa abaixo precisa de prefixo `@`. |

---

## Tasks (Prioritized)

### 🔴 RISKY (Do First)

- [x] **T1 — Mover os 10 arquivos para `get_started/`.** Usar `git mv` (não copiar+deletar) para preservar histórico: `git mv START_HERE.md get_started/START_HERE.md` (repetir para os 10 arquivos listados em Context). Criar a pasta implicitamente via `git mv` (git cria o diretório).
  Verify: `git status` mostra 10 entradas renamed para `get_started/<arquivo>`; `ls get_started/` lista os 10 arquivos; `ls *.md` na raiz mostra apenas `README.md` e `CONTEXT.md` (mais `CLAUDE.md` que não tem extensão dupla mas confira com `ls C*.md`).

### 🟡 CORE

- [x] **T2 — Atualizar `README.md` (raiz).** Trocar `` [`START_HERE.md`](START_HERE.md) `` por `` [`get_started/START_HERE.md`](get_started/START_HERE.md) ``; trocar menção a `` `SDD_Guia_Comandos.md` `` por `` `get_started/SDD_Guia_Comandos.md` ``; adicionar uma linha/link apontando para `get_started/README.md` (o índice criado em T9).
  Verify: `grep -n "get_started/" README.md` retorna pelo menos 3 linhas.

- [x] **T3 — Atualizar `CONTEXT.md` (raiz).** Trocar as menções de `` `START_HERE.md` ``, `` `DEV_LOOP_Guia_Comandos.md` ``, `` `SDD_Guia_Comandos.md` `` por `` `get_started/START_HERE.md` ``, `` `get_started/DEV_LOOP_Guia_Comandos.md` ``, `` `get_started/SDD_Guia_Comandos.md` ``.
  Verify: `grep -n "get_started/" CONTEXT.md` retorna 3 linhas.

- [x] **T4 — Atualizar `docs/00-INDEX.md`.** Inserir `get_started/` logo após `../` nestes 5 links: `../START_HERE.md`, `../DEV_LOOP_Guia_Comandos.md`, `../SDD_Guia_Comandos.md`, `../HOW_TO_USE_AGENTS.md`, `../AGENTIC_UPGRADE_Guia.md` (texto do link e destino). **Não** alterar `../README.md` nem `../CONTEXT.md` (continuam na raiz).
  Verify: `grep -c "\.\./get_started/" docs/00-INDEX.md` ≥ 5; `grep -E "\]\(\.\./(START_HERE|DEV_LOOP_Guia_Comandos|SDD_Guia_Comandos|HOW_TO_USE_AGENTS|AGENTIC_UPGRADE_Guia)\.md\)" docs/00-INDEX.md` não retorna nada.

- [x] **T5 — Corrigir links de saída em `get_started/DEV_LOOP_Guia_Comandos.md`.** Adicionar `../` antes de todo link para fora de `get_started/` (destinos: `.cursor/commands/dev/dev.md`; `.cursor/agents/communication/metting-analyst.md` [2 ocorrências]; `.cursor/agents/communication/the-planner.md` [2 ocorrências]; `.cursor/commands/workflow/design.md`; `.cursor/agents/dev/prompt-crafter.md`; `.cursor/agents/dev/dev-loop-executor.md` [2 ocorrências]; `.cursor/commands/core/router.md`; `.cursor/commands/workflow/build.md`). Links **entre** arquivos movidos (`HOW_TO_USE_AGENTS.md`, `SDD_Guia_Comandos.md`, `START_HERE.md`) ficam sem alteração (mesma pasta).
  Verify: `grep -c "\](\.\./\.cursor/" get_started/DEV_LOOP_Guia_Comandos.md` ≥ 10; `grep "\](\.cursor/" get_started/DEV_LOOP_Guia_Comandos.md` não retorna nada.

- [x] **T6 — Corrigir links de saída em `get_started/AGENTIC_UPGRADE_Guia.md`.** Adicionar `../` nos destinos: `agentspec/INSTALL_UPGRADE.md` [2x]; `docs/DATABRICKS_TEAMS_PIPELINES.md`; `.cursor/kb/teams-notifications/patterns/databricks-ambientes-e-smoke-test.md`; `.cursor/kb/databricks/patterns/pipeline-failure-teams-no-retry.md`; `.cursor/commands/intake.md`; `docs/00-INDEX.md`; `agentspec/INSTALL_AGENTSPEC.md`.
  Verify: `grep -E "\]\((agentspec|docs|\.cursor)/" get_started/AGENTIC_UPGRADE_Guia.md` não retorna nada (tudo deve ter `../` na frente).

- [x] **T7 — Corrigir links de saída em `get_started/HOW_TO_USE_AGENTS.md`.** Adicionar `../` nos destinos: `.cursor/commands/core/router.md` [2x]; `.cursor/commands/core/agent.md` [2x]; `.cursor/commands/intake.md`; `.claude/CLAUDE.md`; `.github/copilot-instructions.md` [2x]; `.cursor/kb/icatu-slides/fluxo-agentes.md`; `.cursor/kb/migration-hub/index.md`; `.cursor/kb/airflow/index.md`; `.cursor/kb/design-pipelines-patterns/index.md`; `.cursor/kb/design-automation-project-patterns/index.md`; `.cursor/kb/dev-python-engineering/index.md`; `.cursor/kb/dev-python-automation/index.md`; `.cursor/agents/ai-ml/ai-data-engineer.md`. Além disso trocar `` [`CLAUDE.md`](CLAUDE.md) `` por `` [`CLAUDE.md`](../CLAUDE.md) ``, `` [`.cursorrules`](.cursorrules) `` por `` [`.cursorrules`](../.cursorrules) `` e `` [`CONTEXT.md`](CONTEXT.md) `` por `` [`CONTEXT.md`](../CONTEXT.md) `` (esses 3 ficam na raiz). Links para `START_HERE.md` e `MANUAL_AGENTES.md` (mesma pasta) ficam sem alteração.
  Verify: `grep -E "\]\((\.cursor|\.claude|\.github)/" get_started/HOW_TO_USE_AGENTS.md` não retorna nada; `grep -E "\]\((CLAUDE\.md|\.cursorrules|CONTEXT\.md)\)" get_started/HOW_TO_USE_AGENTS.md` não retorna nada.

- [x] **T8 — Corrigir links de saída em `get_started/HOW_TO_MAKE_NEW_AGENT.md`.** Adicionar `../` nos destinos: `.cursor/agents/data-engineering/spark-expert-on-premises.md` [2x]; `docs/GUIA_CRIACAO_AGENTE_PROJETO.md`; `.cursor/agents/_template/agent.template.md`; `.cursor/commands/core/router.md`. Links para `MANUAL_AGENTES.md`/`HOW_TO_USE_AGENTS.md` (mesma pasta) ficam sem alteração.
  Verify: `grep -E "\]\((\.cursor|docs)/" get_started/HOW_TO_MAKE_NEW_AGENT.md` não retorna nada.

- [x] **T9 — Criar `get_started/README.md`** com uma tabela "Arquivo | Descrição | Quando usar" cobrindo os 10 arquivos movidos (padrão de `docs/00-INDEX.md`). Adicionar link de volta para `../README.md`.
  Verify: `test -f get_started/README.md`; a tabela tem 10 linhas de dados (uma por arquivo).

- [x] **T10 — Atualizar 6 arquivos nos mirrors `.cursor/`, `.claude/`, `.github/`**: `agents/workflow-dev-loop/devloop-phase.md` e `commands/workflow-dev-loop/workflow-dev-loop.md` em cada uma das 3 árvores. Trocar `` [`DEV_LOOP_Guia_Comandos.md`](../../../DEV_LOOP_Guia_Comandos.md) `` por `` [`DEV_LOOP_Guia_Comandos.md`](../../../get_started/DEV_LOOP_Guia_Comandos.md) ``.
  Verify: `grep -rn "\.\./\.\./\.\./DEV_LOOP_Guia_Comandos\.md" .cursor .claude .github` não retorna nada; `grep -rc "\.\./\.\./\.\./get_started/DEV_LOOP_Guia_Comandos\.md" .cursor .claude .github` soma 6.

- [x] **T11 — Atualizar 3 `agents/exploration/agent-architect.md` (`.cursor`, `.claude`, `.github`).** Trocar `../../../HOW_TO_MAKE_NEW_AGENT.md`, `../../../HOW_TO_USE_AGENTS.md` (2x cada arquivo) e `../../../MANUAL_AGENTES.md` por seus equivalentes com `get_started/` inserido antes do nome do arquivo.
  Verify: `grep -rE "\.\./\.\./\.\./(HOW_TO_MAKE_NEW_AGENT|HOW_TO_USE_AGENTS|MANUAL_AGENTES)\.md" .cursor .claude .github | grep -v get_started` não retorna nada.

- [x] **T12 — Atualizar 3 `commands/workflow/create-pr.md` (`.cursor`, `.claude`, `.github`).** Trocar `` [`HOW_TO_USE_AGENTS.md`](../../../HOW_TO_USE_AGENTS.md) `` por `` [`HOW_TO_USE_AGENTS.md`](../../../get_started/HOW_TO_USE_AGENTS.md) ``.
  Verify: `grep -rn "\.\./\.\./\.\./HOW_TO_USE_AGENTS\.md" .cursor .claude .github` não retorna nada.

- [x] **T13 — Atualizar `.cursor/dev/_index.md`** (apenas esta árvore — `.claude/dev/_index.md` e `.github/dev/_index.md` não têm essa seção; drift pré-existente, não corrigir aqui). Trocar `` [`DEV_LOOP_Guia_Comandos.md`](../../DEV_LOOP_Guia_Comandos.md) `` por `` [`DEV_LOOP_Guia_Comandos.md`](../../get_started/DEV_LOOP_Guia_Comandos.md) ``.
  Verify: `grep -n "get_started/DEV_LOOP_Guia_Comandos" .cursor/dev/_index.md` retorna 1 linha.

- [x] **T14 — Corrigir `scripts/sync-workflow-bundle.sh` linha ~14.** Trocar `GUIDE_ROOT="${REPO_ROOT}/DEV_LOOP_Guia_Comandos.md"` por `GUIDE_ROOT="${REPO_ROOT}/get_started/DEV_LOOP_Guia_Comandos.md"`.
  Verify: `grep -n "GUIDE_ROOT=" scripts/sync-workflow-bundle.sh` mostra o novo caminho.

- [x] **T15 — Corrigir `scripts/validate-workflow-bundle.py` linha ~80.** Trocar `guide_root = REPO_ROOT / "DEV_LOOP_Guia_Comandos.md"` por `guide_root = REPO_ROOT / "get_started" / "DEV_LOOP_Guia_Comandos.md"`.
  Verify: `grep -n "guide_root =" scripts/validate-workflow-bundle.py` mostra o novo caminho.

- [x] **T16 — Corrigir `scripts/validate-agentic-template.py` (`MAIN_FILES`, linhas ~33-45).** Trocar `'START_HERE.md'` por `'get_started/START_HERE.md'` e `'SDD_Guia_Comandos.md'` por `'get_started/SDD_Guia_Comandos.md'`. **Não** alterar a linha ~177 (exceção nominal `'COMO_TORNAR_PROJETO_AGENTIC.md'` em `validate_old_template_names` — checagem por nome de arquivo, continua válida).
  Verify: `grep -n "get_started/START_HERE.md\|get_started/SDD_Guia_Comandos.md" scripts/validate-agentic-template.py` retorna 2 linhas.

### 🟢 POLISH (Do Last)

- [x] **T17 — Ressincronizar o bundle do instalador.** (`rsync` indisponível neste ambiente Windows — substituído por `cp -a`/`cp -f` equivalente à semântica de `rsync -a --delete`; resultado validado por `validate-workflow-bundle.py`.) Rodar `bash scripts/sync-workflow-bundle.sh --dry-run` primeiro para conferir a saída, depois `bash scripts/sync-workflow-bundle.sh` (sem `--dry-run`) para regenerar `install_dev_loop/assets/DEV_LOOP_Guia_Comandos.md` e `install_dev_loop/assets/dev/_index.md` a partir do novo caminho. **Depende de T1, T14 e T15 concluídas.**
  Verify: comando termina com "sincronização concluída" e a validação interna (`validate-workflow-bundle.py`) não reporta erro.

- [x] **T18 — Validação isolada.** Rodar `python3 scripts/validate-workflow-bundle.py` e `python3 scripts/validate-agent-router.py`.
  Verify: ambos saem com exit code 0.

- [x] **T19 — Verificação final ampla.** (encontrou e corrigiu 1 gap não mapeado no design: `.cursor/commands/intake.md` linhas 133/149 com link `../../AGENTIC_UPGRADE_Guia.md` sem `get_started/`.) Para cada um dos 10 nomes de arquivo movidos, rodar grep em toda a árvore **excluindo** `.git`, `.claude/worktrees`, `install_dev_loop`, `agentspec`, `templates`, e confirmar que toda ocorrência de link real (`](...NOME.md)`) já contém `get_started/` no caminho.
  Verify: `grep -rlE "\]\([^)]*(START_HERE|DEV_LOOP_Guia_Comandos|SDD_Guia_Comandos|HOW_TO_USE_AGENTS|HOW_TO_MAKE_NEW_AGENT|MANUAL_AGENTES|AGENTIC_UPGRADE_Guia|COMO_TORNAR_PROJETO_AGENTIC|CRIAR_AMBIENTE_VIRTUAL)\.md\)" --include="*.md" . 2>/dev/null | grep -v -e '\.git/' -e '\.claude/worktrees/' -e 'install_dev_loop/' -e 'agentspec/' -e 'templates/' -e 'get_started/'` não retorna nenhum arquivo.

---

## Exit Criteria

- [x] `ls *.md` na raiz lista apenas `README.md` e `CONTEXT.md` (mais `CLAUDE.md`, sem extensão dupla, confira com `ls C*.md`) — confirmado: `CLAUDE.md`, `CONTEXT.md`, `README.md`.
- [x] `test -d get_started` e `ls get_started/*.md | wc -l` = 11 (10 movidos + `README.md` do índice) — confirmado: 11.
- [x] `python3 scripts/validate-agent-router.py` → exit 0 — `validate-agent-router: OK (77 agentes, 32 hints)`.
- [x] `python3 scripts/validate-workflow-bundle.py` → exit 0 — `validate-workflow-bundle: OK (7 paths de workflow + assets)`.
- [x] `python3 install_dev_loop/install_dev_loop.py --dry-run --target .` → exit 0 (verificação de regressão padrão do projeto) — exit 0 com `PYTHONIOENCODING=utf-8` (console Windows cp1252 não suporta os caracteres do banner; falha de encoding pré-existente do script, não relacionada à reorganização).
- [x] Verificação ampla de T19 não retorna nenhum arquivo com link quebrado — limpo após corrigir `.cursor/commands/intake.md` (gap fora do mapeamento original do design).

---

## Progress

**Status:** COMPLETE

Ver detalhes por iteração em [`../progress/PROGRESS_dl-2026-08-17-get-started.md`](../progress/PROGRESS_dl-2026-08-17-get-started.md) e log final em [`../logs/`](../logs/).

| Iteration | Timestamp | Task Completed | Key Decision | Files Changed |
|-----------|-----------|----------------|--------------|---------------|
| 1 | 2026-08-17T00:00:00Z | T1-T19 (todas) | Ver PROGRESS para detalhe por tarefa | ~35 arquivos (ver LOG) |

---

## Config

```yaml
mode: hitl                # hitl (human-in-loop) | afk (autonomous)
quality_tier: production
max_iterations: 25
max_retries: 3
circuit_breaker: 3
small_steps: true
feedback_loops:
  - python3 scripts/validate-agent-router.py
  - python3 scripts/validate-workflow-bundle.py
```

---

## Notes

- Requisito do usuário: "organizar todos os arquivos introdutórios da raiz do projeto em um
  get_started, além de diminuir a poluição visual".
- Decisão-chave do requirements: `CONTEXT.md` **fica na raiz** (correção do briefing inicial —
  papel de "contexto rápido sempre visível" reconhecido pelas regras do projeto), diferente do que
  havia sido sugerido antes da fase de requirements.
- Achado crítico do requirements/design: 3 scripts de tooling (`sync-workflow-bundle.sh`,
  `validate-workflow-bundle.py`, `validate-agentic-template.py`) têm paths hardcoded para os
  arquivos movidos — por isso T14-T18 são obrigatórios, não opcionais.
- Ordem de dependência: T1 bloqueia T2-T19; T14+T15 bloqueiam T17; T17 bloqueia T18 fazer sentido
  (embora T18 possa rodar isoladamente a qualquer momento após T14/T15/T1).

---

## References

- [Requirements](../../sdd/dev-loop-runs/dl-2026-08-17-get-started/artifacts/dl-2026-08-17-get-started_requirements.md)
- [Design](../../sdd/dev-loop-runs/dl-2026-08-17-get-started/artifacts/dl-2026-08-17-get-started_design.md)
- [Manifest do RUN](../../sdd/dev-loop-runs/dl-2026-08-17-get-started/DEVLOOP_MANIFEST.yaml)
